name: Documentation

on:
  push:
    branches: [development]
  pull_request:
    branches: [development]

jobs:
  deploy:
    # Reusable workflow defaults the source folder to `docs/` — the
    # Docusaurus site now lives there (migrated from `docusaurus/`).
    uses: ConductionNL/.github/.github/workflows/documentation.yml@main
    # A reusable workflow receives NO secrets by default. Without this block
    # `secrets.CF_API_TOKEN` is empty inside the callee, its "Publish to the
    # Cloudflare Worker" step skips itself on its own guard, and the run
    # finishes GREEN having written only gh-pages — which nothing serves.
    # Measured on run 32715324775: all three jobs green, "Deploy to GitHub
    # Pages" success, "Publish to the Cloudflare Worker" SKIPPED, and the warn
    # step reporting that planix.conduction.nl was not updated. The live site
    # never changes and no check goes red to say so.
    #
    # Mapped explicitly rather than `secrets: inherit`, because `inherit`
    # hands the callee EVERY secret this repo holds — signing cert and key,
    # appstore token, deploy keys — for the sake of two Cloudflare values.
    # This way only those two cross the boundary.
    #
    # The exposure above is the ONLY reason for the explicit mapping. The
    # names are the same on both sides: the org secrets really are
    # `CF_API_TOKEN` / `CF_ACCOUNT_ID` — the names ConductionNL/.github's own
    # deploy-docs.yml reads directly, and the names the callee declares under
    # `workflow_call.secrets`.
    #
    # This block used to read `secrets.CLOUDFLARE_API_TOKEN` /
    # `secrets.CLOUDFLARE_ACCOUNT_ID`, which are not secrets anywhere in this
    # org. Mapping from a name that does not exist is NOT an error — it
    # yields an empty string — so the callee's publish step skipped itself on
    # its own guard and the run still finished green. Measured on run
    # 32760529026, the first run AFTER the mapping merged: "Publish to the
    # Cloudflare Worker" SKIPPED again, the log showing `CF_API_TOKEN:` with
    # no value.
    secrets:
      CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
      CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
    with:
      # FROZEN on the old host: docs/docusaurus.config.js `url:` and
      # docs/static/CNAME both still say `planix.conduction.nl`. This moves to
      # `planninq.conduction.nl` together with those two, not before — the
      # callee's live-site check fetches this host.
      cname: planix.conduction.nl

      # EVERY host this site answers on, in FULL. This was MISSING, and that
      # was not a cosmetic gap: `docs-hosts` defaults to `cname` alone, and
      # wrangler reconciles the worker's triggers against what it is given —
      # so the first successful deploy would have REMOVED
      # `planninq.conduction.nl` from the worker and taken the new host down.
      # Both were attached as custom domains on 2026-08-23 and both serve 200.
      docs-hosts: planix.conduction.nl,planninq.conduction.nl

      # The worker that holds both custom domains. It happens to match what
      # the callee would derive from `cname` today, which is exactly why it is
      # pinned here: the moment `cname` moves to `planninq.conduction.nl` the
      # derived name silently becomes `planninq-docs`, a worker that does not
      # exist, and the deploy forks off a second worker while both custom
      # domains keep routing to this one — green, and reaching nobody.
      worker-name: planix-docs
